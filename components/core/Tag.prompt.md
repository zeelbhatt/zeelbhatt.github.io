Tag — a monospace pill for skills, tech stacks and small metadata labels.

```jsx
<Tag>PyTorch</Tag>
<Tag tone="accent">WACV 2026</Tag>
```

Tones: `neutral` (subtle gray, bordered) and `accent` (blue tint). Text never wraps. Use for skill lists, project tech tags, publication venues.
